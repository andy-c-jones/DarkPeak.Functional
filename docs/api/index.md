# API Reference

Browse the full API documentation for DarkPeak.Functional, generated from XML documentation comments in the source code.

## Namespaces

- **DarkPeak.Functional** — Core monadic types: `Option<T>`, `Result<T, TError>`, `Either<TLeft, TRight>`, `Validation<T, TError>`, error types, retry policies, and memoization.
- **DarkPeak.Functional.Extensions** — Extension methods for type conversions, LINQ-style operations on collections, and fluent async chaining via `Task<T>` extensions.
